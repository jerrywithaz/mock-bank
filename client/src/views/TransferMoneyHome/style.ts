import styled from 'styled-components';
import { Col as AntCol, Typography } from 'antd';

export const TransferMoneyHome = styled.div`
    width: 100%;
    height: 100%;
`;

export const Col = styled(AntCol)`
    padding: 0px 10px;
`;

export const Label = styled(Typography.Text)`
    margin-bottom: 5px;
`;